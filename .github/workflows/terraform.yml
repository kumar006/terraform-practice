name: My First Workflow

on:
  push:
    branches:
      - feature-branch

jobs:
  hello11:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions!"